import { AlpakaTokenInfo } from "@/components/token/AlpakaTokenInfo";

export default function AlpakaTokenPage() {
  return (
    <main className="flex flex-1 flex-col gap-4 p-4 md:gap-8 md:p-8">
      <AlpakaTokenInfo />
    </main>
  );
}
